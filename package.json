{
  "name": "solveo-dev",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"bun run --cwd pibackend dev\" \"npm --prefix ./frontend run dev -- --hostname 0.0.0.0\"",
    "dev:pibackend": "bun run --cwd pibackend dev",
    "dev:frontend": "npm --prefix ./frontend run dev -- --hostname 0.0.0.0",
    "install:all": "npm install && npm --prefix ./frontend install && bun install --cwd pibackend",
    "typecheck:pibackend": "bun run --cwd pibackend typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
